/* NOTE: A prototype declares the bare minimum information to call a function.
 *       In this case, we don't need to know about the code inside add; we just
 *       need to know that it takes two integers and returns an integer. */
int add(int, int);
