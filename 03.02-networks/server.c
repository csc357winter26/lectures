#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
    /* NOTE: This program (the server) will already be running, passively
     *       waiting for other programs (the clients) to connect. */

    return EXIT_SUCCESS;
}
